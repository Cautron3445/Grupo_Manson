coords_x = [2, 8, 8, 2]
coords_y = [1, 1, 5, 5]

# Operación entre el máximo y el mínimo de x
operacion = max(coords_x) - min(coords_x)
print(f"La operación (máximo - mínimo) de x es: {operacion}")

# Operación entre el máximo y el mínimo de x
operacion = max(coords_x) - min(coords_x)
print(f"La operación (máximo - mínimo) de x es: {operacion}")

# Operación entre el máximo y el mínimo de x
operacion = max(coords_x) - min(coords_x)
print(f"La operación (máximo - mínimo) de x es: {operacion}")

# Operación entre el máximo y el mínimo de x
operacion = max(coords_x) - min(coords_x)
print(f"La operación (máximo - mínimo) de x es: {operacion}")

def evaluar_zona_poligono(x,y):
